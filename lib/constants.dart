import 'package:flutter/material.dart';

const mPrimaryGradientColor = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Color(0xFF259EA1), Color(0xFF00509B)]);

const mSecondGradientColor = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Color(0xFFFF0000), Color(0xFFBC0505)]);

const mPrimaryColor = Color(0xDD909090);
const mSecondColor = Color(0xFFF1F0F5);